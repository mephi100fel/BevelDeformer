# BevelDeformer — план разработки

Этот файл — рабочий план (не changelog). Обновляем по мере уточнения требований.

## TODO

1. Перейти с ориентации по World X на World Y
   - Сейчас «фронт»/ориентация и логика lock завязаны на World X.
   - Цель: считать «лицевое направление» World Y и пересчитать, какая ось будет locked/active.

2. Выбор Locked оси в World Space
   - UI: `Auto` / `World X` / `World Y` / `World Z`.
   - Поведение:
     - `Auto` — выбирает ось по максимальному совпадению ориентации объекта с выбранной мировой осью.
     - Явный выбор — принудительно лочит выбранную мировую ось.

3. Удобное управление плотностью/градиентом деформации для выделенного lattice
   - Идея: контроллер/гизмо на углу lattice, влияющий на крайние ряды по горизонтали/вертикали.
   - Цель: быстро получать «мягкий градиент» перемещения без ручного ковыряния точек.
   - Требуется уточнение UX:
     - что именно контролируем (смещение предпоследних рядов? кривизну? falloff?)
     - где хранить параметры (Scene / Lattice custom props)
     - как применять (live update / кнопка Apply)

4. Сформировать полноценный аддон
   - Перенести текущие операторы/панели в один файл в `addon/`.
   - Единая панель управления, версия, регистрация, стабильные ID.

5. Загрузка библиотеки объектов (камни и т.п.)
   - Цель: подгружать ассеты из внешнего `.blend`/библиотеки без ручного Append.
   - На подумать:
     - путь к библиотеке + список коллекций/объектов
     - «вставить N случайных» в область/по поверхности
     - кеширование/линк vs аппенд
